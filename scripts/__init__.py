"""Deliberate, human-run operations: recording the corpus, probing the live API.

Run them as modules (`uv run python -m scripts.record_corpus`) so the repository root is on the
import path without any script needing to edit `sys.path`.
"""
